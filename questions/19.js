// Flags and property descriptors
// value - the value of the property
// writable – if true, the property can be modified; otherwise, it is read-only
// enumerable – if true, the property is enumerable, otherwise, loops will ignore it
// configurable – f true, the property can be deleted, and these attributes can be changed; otherwise, they cannot
